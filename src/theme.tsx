import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#369C96",
    },
    secondary: {
      main: "#304399",
    },
  },
});

export default theme;
